
@import 'nib'

/** 页面头部 */
#page-header
  // 样式定义在下方
  background: transparent !important

  &.full_page
    // Hero gradient overlay for better text readability
    &:before
      content: ''
      position: absolute
      inset: 0
      background: linear-gradient(180deg, rgba(16, 20, 36, 0.28) 0%, rgba(16, 20, 36, 0.12) 40%, rgba(16, 20, 36, 0.32) 100%)
      z-index: 0

    #site-info
      position: relative
      z-index: 1

    #site-title
      font-size: 2.8rem
      font-weight: 800
      letter-spacing: -.02em
      text-shadow: 0 2px 12px rgba(0,0,0,0.18)

    #site-subtitle
      font-size: 1.1rem
      opacity: .9

    #scroll-down
      z-index: 1

      .scroll-down-effects
        font-size: 1.5rem
        animation: pasuleScrollBounce 2s ease-in-out infinite

  @keyframes pasuleScrollBounce
    0%, 100%
      transform: translateY(0)
      opacity: .7
    50%
      transform: translateY(10px)
      opacity: 1

  &:not(.full_page):before
    background: transparent !important

  &.post-bg, &.not-home-page
    height: 280px !important

  #post-info
    bottom: 40px !important

  #page-site-info
    top: 140px !important

  @media screen and (max-width: 768px)
    &.not-home-page
      height: 200px !important
    #post-info
      bottom: 10px !important
    #page-site-info
      top: 100px !important

.top-img
  height: 250px
  margin: -50px -40px 50px
  border-top-left-radius: inherit
  border-top-right-radius: inherit
  background-position: center center
  background-size: cover
  transition: all 0.3s

  @media screen and (max-width: 768px)
    height: 230px
    margin: -36px -14px 36px

  [data-theme='dark'] &
    filter: brightness(0.8)

// 页脚
#footer:before
  background-color: rgba(255, 255, 255, 0.5)

  [data-theme='dark'] &
    background-color: rgba(0, 0, 0, 0.5)

#footer-wrap, #footer-wrap a
  color: #111
  transition: unset

  [data-theme='dark'] &
    color: var(--light-grey)

:root
  --pasule-ink: #1a2332
  --pasule-muted: #5b6e8c
  --pasule-line: rgba(114, 128, 167, 0.14)
  --pasule-pink: #f8588c
  --pasule-blue: #5b8def
  --pasule-purple: #7c5ce7
  --pasule-cream: #fff
  --pasule-shadow: 0 8px 32px rgba(92, 101, 138, 0.12)
  --pasule-shadow-lg: 0 20px 48px rgba(92, 101, 138, 0.16)
  --pasule-glass-bg: #fff
  --pasule-glass-border: #e6edf7
  --pasule-card-bg: #fff
  --pasule-gradient-warm: linear-gradient(135deg, rgba(255, 220, 235, 0.95), rgba(255, 240, 245, 0.92))
  --pasule-gradient-cool: linear-gradient(135deg, rgba(225, 238, 255, 0.95), rgba(240, 245, 255, 0.92))
  --pasule-radius-sm: 16px
  --pasule-radius: 22px
  --pasule-radius-lg: 28px

#nav
  backdrop-filter: none
  -webkit-backdrop-filter: none

/* 全局容器更明亮 */
.content-wrap
  background: #fff !important
  backdrop-filter: none !important
  -webkit-backdrop-filter: none !important
  box-shadow: 0 6px 22px rgba(92, 101, 138, 0.08) !important
  border-radius: 18px !important
  border: 1px solid #e6edf7 !important

/* 侧边栏卡片更明亮 */
#aside-content .card-widget
  background: #fff !important
  backdrop-filter: none !important
  -webkit-backdrop-filter: none !important
  box-shadow: 0 6px 20px rgba(92, 101, 138, 0.08) !important
  border-radius: 14px !important
  border: 1px solid #e6edf7 !important

/* 修复侧边栏卡片标题旋转问题 */
#aside-content .item-headline
  writing-mode: horizontal-tb !important
  transform: none !important
  padding: 13px 16px 9px !important
  border-bottom: 1px solid rgba(79, 125, 243, 0.13) !important
  margin: 0 16px 12px !important
  font-weight: 700 !important
  color: var(--pasule-purple, #7c5ce7) !important
  font-size: 0.9rem !important
  letter-spacing: 0 !important
  width: auto !important
  height: auto !important
  position: relative !important

/* 确保卡片标题图标正常显示 */
#aside-content .item-headline i
  width: auto !important
  height: auto !important
  line-height: 1 !important
  transform: none !important

/* 修复侧边栏文章列表项垂直旋转问题 */
#aside-content .aside-list,
#aside-content .aside-list-item
  writing-mode: horizontal-tb !important
  transform: none !important

#aside-content .aside-list-item .content
  writing-mode: horizontal-tb !important
  transform: none !important
  text-align: left !important

#aside-content .aside-list-item .name
  writing-mode: horizontal-tb !important
  transform: none !important
  text-align: left !important
  line-height: 1.5 !important
  font-weight: 500 !important

#aside-content .aside-list-item .datetime
  writing-mode: horizontal-tb !important
  transform: none !important
  text-align: left !important

#aside-content .card-archive-list-count,
#aside-content .card-archive-list-date
  writing-mode: horizontal-tb !important
  transform: none !important

.pasule-link-stack
  display: grid
  gap: 8px

  a
    display: flex
    align-items: center
    justify-content: center
    min-height: 38px
    padding: 8px 10px
    border-radius: 12px
    background: var(--pasule-surface-soft)
    border: 1px solid var(--pasule-line)
    color: var(--pasule-ink)
    font-size: .88rem
    font-weight: 650
    transition: all .2s ease

    &:hover
      transform: translateY(-1px)
      color: var(--pasule-accent)
      box-shadow: var(--pasule-shadow)

.pasule-widget-copy
  margin: 0
  color: var(--pasule-muted)
  line-height: 1.7

.pasule-project-page,
.pasule-about-page,
.pasule-post-guide
  color: var(--pasule-ink)

.pasule-project-page,
.pasule-about-panel,
.pasule-post-guide
  border-radius: var(--pasule-radius-lg)
  border: 1px solid var(--pasule-line)
  background: var(--pasule-card-bg)
  box-shadow: var(--pasule-shadow)

.pasule-card-kicker
  margin: 0 0 8px
  font-size: .75rem
  font-weight: 700
  letter-spacing: .1em
  text-transform: uppercase
  color: var(--pasule-purple)

.pasule-primary-link,
.pasule-secondary-link,
.pasule-mini-link
  display: inline-flex
  align-items: center
  justify-content: center
  border-radius: 999px
  padding: 10px 16px

.pasule-primary-link
  color: #fff
  background: linear-gradient(135deg, var(--pasule-pink), var(--pasule-purple))
  padding: 10px 20px
  border-radius: 999px
  font-weight: 600
  transition: transform .2s ease, box-shadow .2s ease

  &:hover
    transform: translateY(-2px)
    box-shadow: 0 6px 20px rgba(120, 80, 200, 0.3)

.pasule-secondary-link,
.pasule-mini-link
  color: var(--pasule-ink)
  background: var(--pasule-surface-soft)
  border: 1px solid var(--pasule-line)

.pasule-card-grid,
.pasule-about-grid
  display: grid
  gap: 16px

.pasule-card-grid
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr))

.pasule-section-head
  display: flex
  align-items: center
  justify-content: space-between
  gap: 12px
  margin-bottom: 20px
  padding-bottom: 14px
  border-bottom: 2px solid var(--pasule-line)

  h2
    margin: 0
    font-size: 1.35rem
    font-weight: 700
    letter-spacing: -.01em

  a
    font-size: .88rem
    color: var(--pasule-blue)
    font-weight: 600

.pasule-project-card,
.pasule-about-panel
  padding: 20px
  border-radius: var(--pasule-radius-sm)
  border: 1px solid var(--pasule-line)
  background: var(--pasule-glass-bg)
  transition: transform .25s ease, box-shadow .25s ease

  &:hover
    transform: translateY(-3px)
    box-shadow: var(--pasule-shadow-lg)

.pasule-mini-link-list,
.pasule-stack-list
  display: flex
  flex-wrap: wrap
  gap: 10px
  margin-top: 12px

.pasule-project-status
  display: inline-block
  margin-top: 8px
  padding: 4px 10px
  border-radius: 999px
  font-size: .78rem
  font-weight: 600
  background: rgba(124, 92, 231, 0.1)
  color: var(--pasule-purple)
  border: 1px solid rgba(124, 92, 231, 0.2)

.pasule-post-guide
  margin-bottom: 24px
  padding: 18px 20px

.pasule-guide-links
  display: flex
  flex-wrap: wrap
  gap: 10px
  margin-top: 14px

@media screen and (max-width: 768px)
  .pasule-project-page,
  .pasule-about-panel,
  .pasule-post-guide
    border-radius: 18px

body
  --pasule-nav-pill-bg: var(--pasule-surface-soft)
  --pasule-nav-pill-border: var(--pasule-border)

#nav[data-pasule-nav-grouped]
  height: 60px
  display: flex
  align-items: center

  #blog_name,
  #menus,
  #nav-right
    height: 100%
    display: flex
    align-items: center

  #blog_name
    min-width: 0

  #menus
    justify-content: flex-end
    gap: 10px

  #name-container
    display: none

  #toggle-menu
    display: none !important
    align-items: center
    justify-content: center
    width: 38px
    height: 38px
    margin: 0
    padding: 0
    border-radius: 999px
    color: var(--pasule-blue)
    line-height: 1

    a,
    .site-page
      display: inline-flex
      align-items: center
      justify-content: center
      width: 100%
      height: 100%

  .menus_items
    display: flex
    align-items: center
    height: 100%
    gap: 6px

  .menus_item
    position: relative
    display: flex
    align-items: center
    height: 100%
    padding: 0 !important
    overflow: visible

    &:hover,
    &:focus-within
      .menus_item_child
        opacity: 1
        visibility: visible
        pointer-events: auto
        transform: translate3d(-50%, 0, 0)

    .menus_item_child
      position: absolute !important
      top: calc(100% + 6px)
      left: 50%
      right: auto
      z-index: 120
      display: block !important
      margin-top: 0
      padding: 6px
      width: max-content
      min-width: 108px
      max-width: calc(100vw - 32px)
      overflow: visible
      border-radius: 10px
      background: var(--pasule-surface-strong)
      box-shadow: 0 12px 30px rgba(22, 39, 78, 0.18)
      opacity: 0
      visibility: hidden
      pointer-events: none
      animation: none
      transform: translate3d(-50%, 8px, 0)
      transition: opacity .18s ease, visibility .18s ease, transform .18s ease

      &:before
        top: -12px
        left: 0
        height: 14px

      li
        overflow: hidden
        border-radius: 7px

        &:hover
          background: rgba(91,141,239,.10)

        a
          display: flex
          align-items: center
          justify-content: center
          min-height: 40px
          padding: 0 14px
          white-space: nowrap
          color: var(--pasule-ink) !important

    &:nth-last-child(-n + 2)
      .menus_item_child
        left: auto
        right: 0
        transform: translate3d(0, 8px, 0)

    &:nth-last-child(-n + 2):hover,
    &:nth-last-child(-n + 2):focus-within
      .menus_item_child
        transform: translate3d(0, 0, 0)

  .menus_item > .site-page
    display: inline-flex !important
    align-items: center
    justify-content: center
    min-height: 36px
    padding: 0 14px
    border-radius: 999px
    background: var(--pasule-nav-pill-bg) !important
    border: 1px solid var(--pasule-nav-pill-border) !important
    font-weight: 600
    font-size: .9rem
    line-height: 1
    text-shadow: none
    transition: background .2s ease, transform .2s ease

    &:hover
      transform: translateY(-1px)
      background: var(--pasule-surface) !important

    &:after
      display: none

  @media screen and (max-width: 768px)
    #toggle-menu
      display: flex !important

    .menus_items
      display: none

.pasule-nav-group
  display: inline-flex
  align-items: center
  gap: 6px

[data-pasule-announcement-panel]
  padding: 20px
  border-radius: 20px
  border: 1px solid var(--pasule-line)
  background: var(--pasule-surface)
  box-shadow: 0 8px 22px rgba(55, 72, 120, 0.07)

[data-theme='dark']
  --pasule-ink: rgba(235, 240, 255, 0.92)
  --pasule-muted: rgba(200, 212, 245, 0.78)
  --pasule-line: rgba(150, 175, 255, 0.18)
  --pasule-shadow: 0 8px 32px rgba(0, 0, 0, 0.25)
  --pasule-shadow-lg: 0 20px 48px rgba(0, 0, 0, 0.32)
  --pasule-glass-bg: #1c2030
  --pasule-glass-border: rgba(255, 255, 255, 0.1)
  --pasule-card-bg: #1c2030
  --pasule-gradient-warm: linear-gradient(135deg, rgba(35, 30, 50, 0.95), rgba(25, 28, 45, 0.95))
  --pasule-gradient-cool: linear-gradient(135deg, rgba(28, 32, 52, 0.95), rgba(25, 30, 48, 0.95))

  .pasule-project-page,
  .pasule-about-panel,
  .pasule-post-guide,
  #aside-content .card-widget
    background: linear-gradient(180deg, rgba(35, 40, 60, 0.88), rgba(22, 26, 42, 0.92))
    border-color: rgba(150, 175, 255, 0.15)

  .pasule-primary-link
    box-shadow: 0 4px 16px rgba(120, 100, 240, 0.3)

  .pasule-section-head
    border-bottom-color: rgba(120, 150, 220, 0.12)

  .pasule-card-kicker
    color: rgba(170, 190, 255, 0.85)

  /* 暗色模式侧边栏标题 */
  #aside-content .item-headline
    color: #a8b4ff !important
    border-bottom-color: rgba(140, 160, 255, 0.2) !important

:root
  --pasule-home-max: 1280px
  --pasule-side-fab-offset: 170px

.pasule-card-grid
  grid-template-columns: repeat(2, minmax(0, 1fr))

#recent-posts .recent-post-items
  display: grid
  grid-template-columns: repeat(2, minmax(0, 1fr))
  gap: 18px

  .recent-post-item
    width: 100%
    margin: 0

@media screen and (max-width: 1100px)
  .pasule-card-grid,
  #recent-posts .recent-post-items
    grid-template-columns: 1fr

@media screen and (min-width: 901px)
  #rightside
    right: 18px
    bottom: var(--pasule-side-fab-offset)

@media screen and (max-width: 900px)
  #rightside
    right: 14px
    bottom: 92px

.pasule-card-grid
  grid-template-columns: repeat(2, minmax(0, 1fr))
  gap: 12px

#recent-posts .recent-post-items
  grid-template-columns: repeat(3, minmax(0, 1fr)) !important
  gap: 16px

#recent-posts .recent-post-item
  min-height: 0

  .article-title
    font-size: 1.2rem

  .content
    -webkit-line-clamp: 4

@media screen and (min-width: 901px)
  #rightside
    right: 16px !important
    bottom: 220px !important

@media screen and (max-width: 1200px)
  .pasule-card-grid,
  #recent-posts .recent-post-items
    grid-template-columns: repeat(2, minmax(0, 1fr)) !important

@media screen and (max-width: 900px)
  .pasule-card-grid,
  #recent-posts .recent-post-items
    grid-template-columns: 1fr !important

#aside-content .card-tag-cloud
  display: flex
  flex-wrap: wrap
  gap: 10px
  line-height: 1.8

  a
    display: inline-flex
    align-items: center
    justify-content: center
    margin: 0 !important
    padding: 6px 12px
    border-radius: 999px
    background: var(--pasule-glass-bg)
    border: 1px solid var(--pasule-line)
    color: var(--pasule-ink) !important
    font-size: .86rem
    transition: transform .2s ease, box-shadow .2s ease, background .2s ease

    &:hover
      transform: translateY(-2px)
      box-shadow: var(--pasule-shadow)
      background: var(--pasule-cream)

#recent-posts
  margin-top: 8px

#recent-posts .recent-post-items
  grid-template-columns: repeat(2, minmax(0, 1fr)) !important
  gap: 18px !important

#recent-posts .recent-post-item
  display: grid !important
  grid-template-rows: auto 1fr
  align-items: stretch
  overflow: hidden
  min-height: 100%
  border-radius: var(--pasule-radius)
  border: 1px solid var(--pasule-line) !important
  background: var(--pasule-card-bg)
  box-shadow: var(--pasule-shadow)
  transition: transform .25s ease, box-shadow .25s ease

  &:hover
    transform: translateY(-3px)
    box-shadow: var(--pasule-shadow-lg)

#recent-posts .post_cover
  float: none !important
  width: 100% !important
  margin: 0 !important

  a
    display: block
    aspect-ratio: 16 / 9

  img
    width: 100%
    height: 100%
    object-fit: cover

#recent-posts .recent-post-info
  position: relative
  display: flex
  flex-direction: column
  gap: 9px
  min-width: 0
  padding: 18px 20px 20px

#recent-posts .article-title
  display: -webkit-box
  overflow: hidden
  -webkit-box-orient: vertical
  -webkit-line-clamp: 2
  color: var(--pasule-heading)
  font-weight: 760
  line-height: 1.32

#recent-posts .article-meta-wrap
  color: var(--pasule-muted)
  font-size: .82rem
  line-height: 1.55

#recent-posts .article-meta-wrap i
  color: rgba(79, 125, 243, .72)

#recent-posts .content
  display: -webkit-box
  overflow: hidden
  margin: 0
  color: var(--pasule-body)
  font-size: .92rem
  line-height: 1.75
  -webkit-box-orient: vertical
  -webkit-line-clamp: 2

#recent-posts [data-pasule-post-card] .recent-post-info:after
  align-self: flex-start
  margin-top: auto
  padding-top: 2px
  color: var(--pasule-accent)
  font-size: .82rem
  font-weight: 700
  content: '阅读全文 →'

#aside-content .card-info
  padding: 20px 18px 18px !important

  .avatar-img
    width: 78px !important
    height: 78px !important
    margin: 0 auto 10px !important
    border-radius: 50% !important
    box-shadow: 0 8px 20px rgba(36,48,72,.12)

  .author-info-name
    margin-bottom: 4px
    color: var(--pasule-heading)
    font-size: 1.08rem
    font-weight: 760
    line-height: 1.25

  .author-info-description
    max-width: 210px
    margin: 0 auto 14px
    color: var(--pasule-body)
    font-size: .84rem
    line-height: 1.65

  .site-data
    display: grid
    grid-template-columns: repeat(3, minmax(0, 1fr))
    gap: 8px
    margin: 0 0 12px !important

    a
      padding: 8px 4px
      border-radius: 12px
      background: var(--pasule-surface-soft)
      border: 1px solid var(--pasule-border)

    .headline
      color: var(--pasule-muted)
      font-size: .74rem

    .length-num
      color: var(--pasule-heading)
      font-size: 1rem
      font-weight: 760

  #card-info-btn
    min-height: 36px
    margin: 0 0 12px !important
    border-radius: 12px !important
    background: rgba(79,125,243,.12) !important
    color: var(--pasule-accent) !important
    box-shadow: none !important
    font-size: .88rem

  .card-info-social-icons
    margin: 0 !important
    gap: 16px
    font-size: 1.05rem

#aside-content .card-announcement,
#aside-content #pasule-station-note
  .announcement_content,
  .pasule-widget-copy
    padding: 0 18px 18px
    color: var(--pasule-body)
    font-size: .86rem
    line-height: 1.75

#aside-content #pasule-quick-jump .pasule-link-stack
  grid-template-columns: repeat(2, minmax(0, 1fr))
  padding: 0 16px 16px

#aside-content #pasule-quick-jump .item-content
  padding: 0 !important

@media screen and (min-width: 901px)
  #rightside
    right: 16px !important
    bottom: 248px !important

@media screen and (max-width: 1100px)
  #recent-posts .recent-post-items
    grid-template-columns: 1fr !important

@media screen and (max-width: 768px)
  #aside-content .card-tag-cloud
    gap: 8px

    a
      padding: 6px 10px

// Article page second cycle: info cards, reading rail and stronger closure.
#post #article-container
  display: grid
  gap: 18px

.pasule-article-brief
  display: grid
  gap: 18px
  margin-bottom: 4px
  padding: 24px
  border-radius: 24px
  border: 1px solid var(--pasule-line)
  background: var(--pasule-card-bg)
  box-shadow: var(--pasule-shadow)

.pasule-article-brief-main
  display: grid
  gap: 12px

  h2
    margin: 0
    line-height: 1.3

.pasule-article-meta-grid
  display: grid
  grid-template-columns: repeat(4, minmax(0, 1fr))
  gap: 14px

.pasule-brief-stat
  padding: 16px 18px
  border-radius: 18px
  border: 1px solid var(--pasule-line)
  background: var(--pasule-surface-soft)

  h3
    margin: 6px 0 8px
    font-size: 1.35rem
    line-height: 1.2

  p:last-child
    margin: 0
    color: var(--pasule-muted)
    line-height: 1.7

.pasule-inline-progress,
.pasule-reading-progress
  display: grid
  gap: 10px

.pasule-progress-meta
  display: flex
  align-items: center
  justify-content: space-between
  gap: 12px

  span
    color: var(--pasule-muted)

  strong
    font-size: 1.2rem
    color: var(--pasule-ink)

.pasule-progress-track
  width: 100%
  height: 10px
  overflow: hidden
  border-radius: 999px
  background: rgba(111, 94, 142, 0.12)

  span
    display: block
    width: 0
    height: 100%
    border-radius: inherit
    background: linear-gradient(135deg, var(--pasule-pink), var(--pasule-purple))
    transition: width .2s ease

.pasule-post-guide
  padding: 22px

.pasule-post-guide-grid
  display: grid
  grid-template-columns: repeat(3, minmax(0, 1fr))
  gap: 14px

.pasule-guide-panel
  padding: 18px
  border-radius: 20px
  border: 1px solid var(--pasule-line)
  background: var(--pasule-surface-soft)

  h3
    margin: 4px 0 10px
    line-height: 1.35

  p
    margin: 0
    line-height: 1.8
    color: var(--pasule-muted)

.pasule-guide-list
  margin: 0
  padding: 0
  list-style: none
  display: grid
  gap: 10px

  li
    position: relative
    padding-left: 16px
    color: var(--pasule-muted)
    line-height: 1.8

    &:before
      position: absolute
      top: .72rem
      left: 0
      width: 8px
      height: 8px
      border-radius: 999px
      background: linear-gradient(135deg, var(--pasule-pink), var(--pasule-purple))
      content: ''

.pasule-reading-rail
  border-radius: 24px !important
  border: 1px solid var(--pasule-line) !important
  overflow: hidden

.pasule-current-section
  margin: 0
  color: var(--pasule-muted)
  line-height: 1.7

.pasule-reading-rail-grid
  display: grid
  grid-template-columns: repeat(2, minmax(0, 1fr))
  gap: 12px
  margin-top: 14px

.pasule-rail-stat
  padding: 14px 16px
  border-radius: 16px
  border: 1px solid var(--pasule-line)
  background: var(--pasule-surface-soft)
  display: grid
  gap: 6px

  span
    color: var(--pasule-muted)
    font-size: .92rem

  strong
    font-size: 1.1rem
    line-height: 1.2

.pasule-rail-block
  margin-top: 16px

.pasule-rail-links
  display: flex
  flex-wrap: wrap
  gap: 10px
  margin-top: 8px

.pasule-rail-link
  display: inline-flex
  align-items: center
  justify-content: center
  padding: 9px 14px
  border-radius: 999px
  border: 1px solid var(--pasule-line)
  background: var(--pasule-surface-soft)
  color: var(--pasule-ink)
  transition: transform .18s ease, box-shadow .18s ease

  &:hover
    transform: translateY(-2px)
    box-shadow: var(--pasule-shadow)

.pasule-article-outro
  margin-top: 10px
  padding: 22px
  border-radius: 24px
  border: 1px solid var(--pasule-line)
  background: var(--pasule-card-bg)
  box-shadow: var(--pasule-shadow)

.post #aside-content .sticky_layout
  display: grid
  gap: 14px

.post #aside-content #card-toc,
.post #aside-content .card-post-series
  border-radius: 22px !important
  border: 1px solid var(--pasule-line)
  overflow: hidden

.post #aside-content #card-toc .toc-percentage
  display: none

.post #aside-content #card-toc .toc-content
  max-height: 360px
  overflow: auto
  padding-right: 4px

#page-header.post-bg
  #post-info #post-meta
    display: inline-grid
    gap: 8px
    padding: 12px 18px
    border-radius: 18px
    border: 1px solid rgba(255,255,255,.18)
    background: rgba(18, 22, 34, 0.62)

  #post-info .meta-firstline,
  #post-info .meta-secondline
    display: flex
    flex-wrap: wrap
    gap: 8px 10px
    justify-content: center

[data-theme='dark']
  .pasule-article-brief,
  .pasule-article-outro,
  .pasule-reading-rail,
  .pasule-guide-panel,
  .pasule-brief-stat
    background: linear-gradient(180deg, rgba(22,26,40,.76), rgba(14,18,30,.9))

  .pasule-rail-stat,
  .pasule-rail-link
    background: rgba(18,22,34,.74)

  #page-header.post-bg #post-info #post-meta
    background: rgba(9, 12, 22, 0.38)
    border-color: rgba(145, 168, 255, 0.14)

@media screen and (max-width: 1200px)
  .pasule-article-meta-grid
    grid-template-columns: repeat(2, minmax(0, 1fr))

  .pasule-post-guide-grid
    grid-template-columns: 1fr

@media screen and (max-width: 900px)
  .pasule-reading-rail-grid
    grid-template-columns: 1fr 1fr

@media screen and (max-width: 768px)
  .pasule-article-brief,
  .pasule-post-guide,
  .pasule-article-outro
    padding: 18px
    border-radius: 18px

  .pasule-article-meta-grid,
  .pasule-reading-rail-grid
    grid-template-columns: 1fr

  #page-header.post-bg #post-info #post-meta
    padding: 10px 14px

// Pasule polish pass: clearer tokens, calmer reading surfaces, and active navigation.
:root
  --pasule-surface: #fff
  --pasule-surface-strong: #fff
  --pasule-surface-soft: #f8faff
  --pasule-border: rgba(92,108,145,.14)
  --pasule-accent: #4f7df3
  --pasule-accent-soft: rgba(79,125,243,.12)
  --pasule-accent-2: #ec5f93
  --pasule-heading: #142033
  --pasule-body: #2f405a
  --pasule-code-bg: rgba(18,24,38,.92)
  --pasule-focus-ring: rgba(79,125,243,.28)
  --pasule-radius-xs: 10px
  --pasule-radius-card: 18px
  --pasule-ease: cubic-bezier(.2,.8,.2,1)
  --pasule-shadow-soft: 0 10px 28px rgba(36,48,72,.08)
  --pasule-shadow-lift: 0 18px 38px rgba(36,48,72,.13)

.content-wrap,
#aside-content .card-widget,
.pasule-article-brief,
.pasule-post-guide,
.pasule-article-outro,
.pasule-reading-rail,
#recent-posts .recent-post-item
  background: var(--pasule-surface) !important
  border: 1px solid var(--pasule-border) !important
  box-shadow: var(--pasule-shadow-soft) !important
  backdrop-filter: none !important
  -webkit-backdrop-filter: none !important

.pasule-project-card,
.pasule-guide-panel,
.pasule-brief-stat,
.pasule-rail-stat
  background: var(--pasule-surface-soft) !important
  border: 1px solid var(--pasule-border) !important
  box-shadow: none

.pasule-project-card:hover,
#recent-posts .recent-post-item:hover
  box-shadow: var(--pasule-shadow-lift) !important
  transform: translateY(-2px)

#nav[data-pasule-nav-grouped]
  .menus_item.is-current > .site-page,
  .menus_item > .site-page.is-active
    background: var(--pasule-surface) !important
    border-color: rgba(79,125,243,.22) !important
    color: var(--pasule-accent) !important
    box-shadow: 0 8px 22px rgba(79,125,243,.14)

  .menus_item.is-current > .site-page:before
    position: absolute
    right: 12px
    bottom: 5px
    left: 12px
    height: 2px
    border-radius: 999px
    background: linear-gradient(90deg, var(--pasule-accent), var(--pasule-accent-2))
    content: ''

  .menus_item_child
    border: 1px solid var(--pasule-border)
    background: var(--pasule-surface-strong)

    li a.is-active
      color: var(--pasule-accent) !important
      background: var(--pasule-accent-soft)
      font-weight: 700

  .menus_item > .site-page:focus-visible,
  .menus_item_child a:focus-visible
    outline: 3px solid var(--pasule-focus-ring)
    outline-offset: 3px

#post
  color: var(--pasule-body)

#post #article-container
  max-width: 76ch
  margin-inline: auto
  color: var(--pasule-body)
  font-size: 1.02rem
  line-height: 1.9

  > p,
  > ul,
  > ol
    margin: 0 0 1.05rem

  a:not(.pasule-mini-link):not(.pasule-rail-link)
    color: var(--pasule-accent)
    text-decoration: none
    background: linear-gradient(transparent 62%, rgba(79,125,243,.16) 0)

    &:hover
      color: #315fda
      background: linear-gradient(transparent 54%, rgba(79,125,243,.26) 0)

  h1,
  h2,
  h3,
  h4
    color: var(--pasule-heading)
    letter-spacing: 0
    line-height: 1.35
    scroll-margin-top: 86px

  h2
    margin-top: 2.2rem
    padding-bottom: .55rem
    border-bottom: 1px solid var(--pasule-border)
    font-size: 1.55rem

    &:before
      width: 8px
      height: 8px
      border-radius: 999px
      background: linear-gradient(135deg, var(--pasule-accent), var(--pasule-accent-2))

  h3
    margin-top: 1.7rem
    font-size: 1.22rem

  blockquote
    margin: 1.25rem 0
    padding: 16px 18px 16px 20px
    border-left: 4px solid var(--pasule-accent)
    border-radius: 0 var(--pasule-radius-xs) var(--pasule-radius-xs) 0
    background: rgba(79,125,243,.08)
    color: var(--pasule-body)

    p:last-child
      margin-bottom: 0

  .note
    border-radius: var(--pasule-radius-card)
    box-shadow: none

  code:not([class])
    padding: .16em .42em
    border-radius: 7px
    background: rgba(79,125,243,.10)
    color: #315fda
    font-size: .92em

  figure.highlight,
  pre
    border-radius: var(--pasule-radius-card) !important
    box-shadow: 0 12px 28px rgba(18,24,38,.18) !important

  figure.highlight
    overflow: hidden
    border: 1px solid rgba(255,255,255,.10)
    background: var(--pasule-code-bg) !important

  img
    border-radius: var(--pasule-radius-card)

.pasule-article-brief
  grid-template-columns: minmax(0, 1.08fr) minmax(260px, .92fr)
  align-items: start

  .pasule-article-meta-grid
    grid-template-columns: repeat(2, minmax(0, 1fr))

.pasule-article-brief-main h2
  color: var(--pasule-heading)
  font-size: 1.45rem

.pasule-guide-links,
.pasule-mini-link-list,
.pasule-rail-links
  gap: 8px

.pasule-mini-link,
.pasule-rail-link
  min-height: 34px
  border-radius: 999px
  border: 1px solid var(--pasule-border)
  background: var(--pasule-surface-soft)
  color: var(--pasule-body)
  font-weight: 650

  &:hover
    color: var(--pasule-accent)
    border-color: rgba(79,125,243,.24)
    background: var(--pasule-surface)

.pasule-progress-track
  height: 8px
  background: rgba(79,125,243,.12)

  span
    background: linear-gradient(90deg, var(--pasule-accent), var(--pasule-accent-2))

.post #aside-content #card-toc .toc-content
  max-height: 420px

.post #aside-content #card-toc .toc-link
  border-radius: 8px
  padding: 5px 8px

  &.active
    background: var(--pasule-accent-soft)
    color: var(--pasule-accent)
    font-weight: 700

[data-theme='dark']
  --pasule-surface: #181c2a
  --pasule-surface-strong: #1c2131
  --pasule-surface-soft: #23283a
  --pasule-border: rgba(155,176,235,.14)
  --pasule-accent: #8fb0ff
  --pasule-accent-soft: rgba(143,176,255,.14)
  --pasule-accent-2: #ff8db5
  --pasule-heading: rgba(245,248,255,.94)
  --pasule-body: rgba(225,232,248,.82)
  --pasule-code-bg: rgba(9,12,22,.94)
  --pasule-focus-ring: rgba(143,176,255,.3)

  #post #article-container
    color: var(--pasule-body)

    h1,
    h2,
    h3,
    h4
      color: var(--pasule-heading)

    blockquote
      background: rgba(143,176,255,.10)
      color: var(--pasule-body)

    code:not([class])
      background: rgba(143,176,255,.14)
      color: #bad0ff

  .pasule-mini-link,
  .pasule-rail-link
    background: rgba(35,40,58,.74)
    color: var(--pasule-body)

    &:hover
      background: rgba(42,49,70,.92)
      color: var(--pasule-accent)

  #nav[data-pasule-nav-grouped]
    .menus_item.is-current > .site-page,
    .menus_item > .site-page.is-active
      background: rgba(28,33,49,.92) !important
      color: var(--pasule-accent) !important

    .menus_item_child
      background: var(--pasule-surface-strong)

@media screen and (max-width: 900px)
  .pasule-article-brief
    grid-template-columns: 1fr

  #post #article-container
    max-width: 100%

@media screen and (max-width: 768px)
  #post #article-container
    font-size: 1rem
    line-height: 1.82

    h2
      font-size: 1.34rem

  .pasule-article-brief .pasule-article-meta-grid
    grid-template-columns: 1fr

body.pasule-home-page #rightside
  right: 18px !important
  bottom: 96px !important

body.pasule-home-page canvas[id^='live2d'],
body.pasule-home-page .live2d-widget-container
  transform: scale(.52)
  transform-origin: right bottom

// Static Firefly/Xtower-style homepage and global music player.
body.pasule-home-page #page-header[data-pasule-static-banner='true']
  position: relative
  isolation: isolate

  &:before
    position: absolute
    inset: 0
    z-index: 0
    background: linear-gradient(180deg, rgba(12,18,32,.28), rgba(12,18,32,.18) 48%, rgba(12,18,32,.52))
    content: ''

  #nav,
  #site-info,
  #scroll-down
    position: relative
    z-index: 1

.layout.pasule-home-layout
  display: block
  width: 100%
  max-width: none
  padding: 0

.pasule-home-firefly
  display: grid
  grid-template-columns: minmax(210px, 248px) minmax(0, 1fr) minmax(210px, 248px)
  gap: 14px
  width: calc(100vw - 32px)
  max-width: 1200px
  margin: 0 auto 24px
  align-items: start
  color: var(--pasule-ink)

.pasule-home-rail,
.pasule-home-stream
  min-width: 0

.pasule-home-rail
  display: grid
  gap: 12px
  position: sticky
  top: 82px

.pasule-home-rail-card
  width: 100% !important
  margin: 0 !important
  overflow: hidden
  border-radius: 12px !important
  border: 1px solid var(--pasule-line) !important
  background: var(--pasule-card-bg) !important
  box-shadow: 0 4px 14px rgba(92, 101, 138, 0.08) !important

.pasule-home-rail-card .item-headline
  padding: 13px 14px 8px !important

.pasule-home-music-card .item-content
  display: grid
  gap: 12px
  padding: 0 16px 16px

.pasule-home-music-open
  display: inline-flex
  align-items: center
  justify-content: center
  min-height: 34px
  padding: 0 12px
  border: 1px solid var(--pasule-border)
  border-radius: 10px
  background: var(--pasule-accent-soft)
  color: var(--pasule-accent)
  font-size: .84rem
  font-weight: 700
  cursor: pointer
  transition: transform .2s var(--pasule-ease), box-shadow .2s var(--pasule-ease)

  &:hover
    transform: translateY(-1px)
    box-shadow: var(--pasule-shadow-soft)

.pasule-home-stream-head
  display: flex
  align-items: center
  justify-content: space-between
  gap: 12px
  margin: 0 0 12px
  padding: 12px 14px
  border-radius: 12px
  border: 1px solid var(--pasule-line)
  background: var(--pasule-card-bg)
  box-shadow: 0 4px 14px rgba(92, 101, 138, 0.08)

  h2
    margin: 0
    color: var(--pasule-heading)
    font-size: 1.15rem
    line-height: 1.3
    letter-spacing: 0

  a
    color: var(--pasule-accent)
    font-size: .86rem
    font-weight: 700

.pasule-home-stream #recent-posts
  width: 100%
  margin: 0 !important

.pasule-home-stream #recent-posts .recent-post-items
  display: grid !important
  grid-template-columns: 1fr !important
  gap: 12px !important

.pasule-home-stream #recent-posts .recent-post-item
  display: grid !important
  grid-template-columns: minmax(0, 1fr) 164px
  grid-template-areas: 'info cover'
  align-items: stretch
  min-height: 154px
  margin: 0 !important
  overflow: hidden
  border-radius: 12px
  border: 1px solid var(--pasule-line) !important
  background: var(--pasule-card-bg)
  box-shadow: 0 4px 14px rgba(92, 101, 138, 0.08)
  transition: transform .2s var(--pasule-ease), box-shadow .2s var(--pasule-ease)

  &:hover
    transform: translateY(-2px)
    box-shadow: var(--pasule-shadow-lift)

.pasule-home-stream #recent-posts .recent-post-info
  grid-area: info
  display: flex
  flex-direction: column
  gap: 8px
  min-width: 0
  padding: 18px 20px

.pasule-home-stream #recent-posts .post_cover
  grid-area: cover
  float: none !important
  width: 100% !important
  height: 100% !important
  margin: 0 !important

  a,
  img
    display: block
    width: 100%
    height: 100%

  img
    object-fit: cover

.pasule-home-stream #recent-posts .article-title
  color: var(--pasule-heading)
  font-size: 1.05rem
  font-weight: 760
  line-height: 1.36
  letter-spacing: 0

.pasule-home-stream #recent-posts .article-meta-wrap
  color: var(--pasule-muted)
  font-size: .8rem
  line-height: 1.55

.pasule-home-stream #recent-posts .content
  display: -webkit-box
  overflow: hidden
  margin: 0
  color: var(--pasule-body)
  font-size: .9rem
  line-height: 1.72
  -webkit-box-orient: vertical
  -webkit-line-clamp: 3

.pasule-home-stream #pagination
  margin-top: 18px

.pasule-home-left .card-category-list,
.pasule-home-right .card-archive-list
  display: grid
  gap: 8px
  margin: 0 !important
  padding: 0 16px 16px !important
  list-style: none

.pasule-home-left .card-category-list-link,
.pasule-home-right .card-archive-list-link
  display: flex
  align-items: center
  justify-content: space-between
  gap: 10px
  min-height: 34px
  padding: 7px 10px
  border-radius: 10px
  border: 1px solid var(--pasule-border)
  background: var(--pasule-surface-soft)
  color: var(--pasule-ink) !important
  font-size: .84rem
  line-height: 1.35

.pasule-home-left .card-category-list-item,
.pasule-home-right .card-archive-list-item
  margin: 0 !important
  list-style: none

.pasule-home-left .card-category-list-name,
.pasule-home-right .card-archive-list-date
  min-width: 0
  overflow: hidden
  text-overflow: ellipsis
  white-space: nowrap

.pasule-home-left .card-category-list-count,
.pasule-home-right .card-archive-list-count
  flex: 0 0 auto
  color: var(--pasule-accent)
  font-weight: 760

.pasule-home-left #card-info-btn
  min-height: 36px
  border-radius: 10px !important
  background: var(--pasule-accent-soft) !important
  color: var(--pasule-accent) !important
  box-shadow: none !important

.pasule-home-left .card-tag-cloud
  display: flex
  flex-wrap: wrap
  gap: 8px
  padding: 0 16px 16px !important

  a
    margin: 0 !important
    padding: 6px 10px
    border-radius: 999px
    border: 1px solid var(--pasule-border)
    background: var(--pasule-surface-soft)
    color: var(--pasule-ink) !important
    font-size: .8rem !important

.pasule-chip-more,
.pasule-more-item a
  font-weight: 700
  color: var(--pasule-accent) !important

.pasule-global-music
  position: fixed
  right: 18px
  bottom: 154px
  z-index: 110
  color: var(--pasule-ink)

.pasule-global-music-toggle,
.pasule-global-music-close
  display: inline-flex
  align-items: center
  justify-content: center
  border: 0
  cursor: pointer

.pasule-global-music-toggle
  width: 44px
  height: 44px
  border-radius: 12px
  background: var(--pasule-surface-strong)
  color: var(--pasule-accent)
  box-shadow: var(--pasule-shadow-soft)
  transition: transform .2s var(--pasule-ease), box-shadow .2s var(--pasule-ease)

  &:hover
    transform: translateY(-2px)
    box-shadow: var(--pasule-shadow-lift)

  &:focus-visible
    outline: 3px solid var(--pasule-focus-ring)
    outline-offset: 3px

.pasule-global-music-panel
  position: absolute
  right: 0
  bottom: 54px
  width: 340px
  max-width: calc(100vw - 80px)
  overflow: hidden
  border-radius: 14px
  border: 1px solid var(--pasule-border)
  background: var(--pasule-surface-strong)
  box-shadow: 0 12px 30px rgba(36,48,72,.14)

  &[hidden]
    display: none

.pasule-global-music-head
  display: flex
  align-items: flex-start
  justify-content: space-between
  gap: 12px
  padding: 16px 16px 10px
  border-bottom: 1px solid var(--pasule-border)

  h2
    margin: 0
    font-size: 1rem
    line-height: 1.3

  p
    margin: 0
    color: var(--pasule-muted)
    line-height: 1.55
    font-size: .84rem

.pasule-global-music-close
  width: 34px
  height: 34px
  border-radius: 10px
  background: var(--pasule-accent-soft)
  color: var(--pasule-accent)

.pasule-global-music-body
  padding: 12px

  .aplayer
    margin: 0
    border-radius: 12px
    box-shadow: none
    background: transparent

.pasule-global-music-empty
  margin: 0
  padding: 14px
  border-radius: 12px
  background: var(--pasule-surface-soft)
  color: var(--pasule-muted)
  line-height: 1.65

[data-theme='dark']
  .pasule-global-music-panel,
  .pasule-global-music-toggle
    background: var(--pasule-surface-strong)
    border-color: var(--pasule-border)

@media screen and (max-width: 900px)
  .pasule-home-firefly
    grid-template-columns: 1fr
    width: calc(100vw - 24px)
    max-width: 100%

  .pasule-global-music
    right: 16px
    bottom: 154px

@media screen and (max-width: 768px)
  .pasule-home-rail
    position: static

  .pasule-home-stream #recent-posts .recent-post-item
    grid-template-columns: 1fr
    grid-template-areas: 'cover' 'info'

  .pasule-home-stream #recent-posts .post_cover
    height: 180px !important

@media (prefers-reduced-motion: reduce)
  .pasule-global-music-toggle
    transition: none

// Soft, calm content background (xtower-style); banner image stays on the header.
#web_bg
  background: linear-gradient(180deg, #f3f6fd 0%, #e9eefb 100%) !important

[data-theme='dark'] #web_bg
  background: #10131e !important
