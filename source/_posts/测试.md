---
title: 测试占位页
date: 2026-04-05 01:24
published: false
---

这个文件用于本地测试，不会出现在正式首页列表中。
测试1
测试2
### 测试3
##### 测试4

```mermaid
graph LR
    JSON[JSON interface]
    JSONGetter[JSONGetter interface]
    MapWrapper[MapWrapper]
    JSONObject[JSONObject]
    JSONArray[JSONArray]
    JSONConfig[JSONConfig]
    JSONUtil[JSONUtil]
    ObjectMapper[ObjectMapper]
    JSONTokener[JSONTokener]
    JSONWriter[JSONWriter]
    JSONConverter[JSONConverter]
    InternalJSONUtil[InternalJSONUtil]
    JSONBeanParser[JSONBeanParser interface]
    JSONSupport[JSONSupport]
    XML[XML]
    JSONXMLParser[JSONXMLParser]
    JSONXMLSerializer[JSONXMLSerializer]

    JSONObject -->|extends| MapWrapper
    JSONObject -.->|implements| JSON
    JSONObject -.->|implements| JSONGetter

    JSONArray -.->|implements| JSON
    JSONArray -.->|implements| JSONGetter

    JSONObject -->|holds config| JSONConfig
    JSONArray -->|holds config| JSONConfig

    JSONUtil -->|create or return| JSONObject
    JSONUtil -->|create or return| JSONArray
    JSONUtil -->|toBean| JSONConverter
    JSONUtil -->|XML convert| XML
    JSONUtil -->|parse trigger| JSONTokener
    JSONUtil -->|write trigger| JSONWriter

    JSONObject -->|map source in ctor| ObjectMapper
    JSONObject -->|createMap validate| InternalJSONUtil
    JSONObject -->|wrap value| JSONUtil
    JSONObject -->|accumulate append| JSONArray
    JSONObject -->|write toJSONString| JSONWriter
    JSONObject -->|getByPath| JSONConverter

    ObjectMapper -->|parse string stream| JSONTokener
    ObjectMapper -->|copy options fill props| InternalJSONUtil
    ObjectMapper -->|fill object| JSONObject
    ObjectMapper -->|fill array| JSONArray

    JSONConverter -->|custom deserialize| JSONBeanParser
    JSONSupport -.->|implements| JSONBeanParser
    JSONSupport -->|parse to bean| JSONConverter

    XML -->|XML to JSONObject| JSONXMLParser
    XML -->|JSON to XML| JSONXMLSerializer
```

```mermaid
graph LR
    A["JSONObject set"] --> B{"key 是否为空"}
    B -->|是| Z["直接返回 this"]
    B -->|否| C{"有 filter 吗"}

    C -->|有| D["MutablePair 先过滤或改写"]
    C -->|无| E["继续"]
    D --> E

    E --> F["读取 ignoreNullValue"]
    F --> G{"value 是 null 且忽略 null 吗"}

    G -->|是| H["remove key"]
    G -->|否| I{"checkDuplicate 且 key 已存在"}

    I -->|是| J["抛 JSONException"]
    I -->|否| K["testValidity"]

    K --> L["wrap value"]
    L --> M["put wrappedValue"]
    M --> N["返回 this"]
```


```mermaid
flowchart LR
A[JSONObject.toString]
--> B[toJSONString(0)]
--> C[toJSONString(indentFactor, filter)]
--> D[StringWriter]
--> E[write(writer, indentFactor, indent, filter)]
--> F[JSONWriter.of(...).beginObj]
--> G[forEach key value]
--> H[jsonWriter.writeField(pair, filter)]
--> I[JSONWriter.writeValueDirect]
--> J{值类型判断}
J -->|JSONObject| K[递归写对象]
J -->|JSONArray| L[递归写数组]
J -->|Date/Temporal| M[按 JSONConfig 格式输出]
J -->|String/Number/Boolean| N[直接写出]
J -->|null| O[按 ignoreNullValue 处理]
K --> P[end]
L --> P
M --> P
N --> P
O --> P
P --> Q[返回 JSON 字符串]

```
